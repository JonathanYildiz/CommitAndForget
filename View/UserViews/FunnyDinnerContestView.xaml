<Page x:Class="CommitAndForget.View.UserViews.FunnyDinnerContestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommitAndForget.View.UserViews"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FunnyDinnerContestView">

  <Page.Resources>
    <ResourceDictionary Source="../../Resources/ResourceDictionary.xaml" />
  </Page.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition/>
      <RowDefinition Height="auto"/>
    </Grid.RowDefinitions>
    <ScrollViewer>
      <ItemsControl ItemsSource="{Binding ImageList}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Image Source="{Binding Image}" Height="100" Margin="0,0,0,10"/>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
    <Button Grid.Row="1" Content="+" Command="{Binding AddImageCommand}" Style="{StaticResource AddButton}"/>
  </Grid>

</Page>

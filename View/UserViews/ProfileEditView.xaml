<Window x:Class="CommitAndForget.View.UserViews.ProfileEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommitAndForget.View.UserViews"
        mc:Ignorable="d"
        Title="ProfileEditView" Height="450" Width="800"
        Style="{StaticResource WindowStyle}">
  
  <Grid>
    <StackPanel>
      <DockPanel>
        <Label>Vorname</Label>
        <TextBox Text="{Binding CurrentUser.FirstName}"/>
      </DockPanel>
      <DockPanel>
        <Label>Nachname</Label>
        <TextBox Text="{Binding CurrentUser.LastName}"/>
      </DockPanel>
      <DockPanel>
        <Label>Straße</Label>
        <TextBox Text="{Binding CurrentUser.Street}"/>
      </DockPanel>
      <DockPanel>
        <Label>Hausnummer</Label>
        <TextBox Text="{Binding CurrentUser.HouseNumber}"/>
      </DockPanel>
      <DockPanel>
        <Label>PLZ</Label>
        <TextBox Text="{Binding CurrentUser.PostalCode}"/>
      </DockPanel>
      <DockPanel>
        <Label>Ort</Label>
        <TextBox Text="{Binding CurrentUser.City}"/>
      </DockPanel>
      <DockPanel>
        <Label>Ort</Label>
        <TextBox Text="{Binding CurrentUser.Password}"/>
      </DockPanel>
      <Button Command="{Binding ConfirmRegistrationCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">OK</Button>
      <Button Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">Abbrechen</Button>
    </StackPanel>
  </Grid>
</Window>

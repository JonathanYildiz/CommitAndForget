<Page x:Class="CommitAndForget.View.UserViews.PaymentView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CommitAndForget.View.UserViews"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaymentView">
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Button Grid.Column="0" Content="Paypal" Command="{Binding PayCommand}" CommandParameter="Paypal" Style="{StaticResource NavigationButton}"/>
    <Button Grid.Column="1" Content="Bar" Command="{Binding PayCommand}" CommandParameter="Cash" Style="{StaticResource NavigationButton}"/>
  </Grid>
</Page>
